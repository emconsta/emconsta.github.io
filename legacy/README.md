# Legacy Archive

This folder contains archived PHP/HTML pages and one-off scripts from earlier versions of the site.

- The Jekyll site builds from Markdown in `pages/` and data in `_data/`.
- Publications are maintained directly in `_data/publications.yml`.
- The contents of `legacy/` are excluded from the built site via `_config.yml`.
